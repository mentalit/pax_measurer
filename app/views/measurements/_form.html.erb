<%= form_with(model: [ @room, measurement]) do |form| %>
  <% if measurement.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(measurement.errors.count, "error") %> prohibited this measurement from being saved:</h2>

      <ul>
        <% measurement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


 <div class="calibration-box">
  <button type="button" id="start-calibration">📐 Calibrate height</button>
  <span id="calibration-status"></span>
</div>

  <div>
  <%= form.label :length %>
  <%= form.text_field :length, data: { measure: "length" } %>
  <button type="button" class="measure-btn" data-field="length">📏</button>
</div>

<div>
  <%= form.label :width %>
  <%= form.text_field :width, data: { measure: "width" } %>
  <button type="button" class="measure-btn" data-field="width">📏</button>
</div>

<div>
  <%= form.label :depth %>
  <%= form.text_field :depth, data: { measure: "depth" } %>
  <button type="button" class="measure-btn" data-field="depth">📏</button>
</div>

 <div>
    <%= form.submit %>
  </div>
<% end %>
