<p style="color: green"><%= notice %></p>

<%= render @room %>

<%= link_to "Measurements", room_measurements_path(@room) %>

<div>
  <%= link_to "Edit this room", edit_room_path(@room) %> |
  <%= link_to "Back to rooms", :back %>

  <%= button_to "Destroy this room", @room, method: :delete %>
</div>
